<%= form_with(model: admin_invoice, local: true) do |form| %>
  <% if admin_invoice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_invoice.errors.count, "error") %> prohibited this admin_invoice from being saved:</h2>

      <ul>
      <% admin_invoice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
