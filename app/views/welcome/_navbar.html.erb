<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <span class="navbar-brand"> 
        <%= image_tag("nav/AEI.png", class: "img-responsive", id: "navbar-logo", size: "110x75") %>
    </span>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-between" id="navbarCollapse">
        <div class="navbar-nav">
            <a href="#" class="nav-item nav-link active">Inicio</a>
            <div class="nav-item dropdown ">
                <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">Categorías</a>
                <div class="dropdown-menu">
                    <a href="#" class="dropdown-item">Inbox</a>
                    <a href="#" class="dropdown-item">Sent</a>
                    <a href="#" class="dropdown-item">Drafts</a>
                </div>
            </div>
            <a href="#" class="nav-item nav-link">Contacto</a>
        </div>
        <!--<form class="form-inline">
            <div class="input-group">
                <div class="container-search">
                    <input class="search__input" type="text" placeholder="Buscar">
                </div>
            </div>
        </form>-->

        <form action="#" class="search-wrap">
            <div class="input-group">
                <input type="text" class="search-input" style="width:55%;" placeholder="Busca productos, categorias y más...">
                <div class="input-group-append">
                    <button class="btn btn-search" type="submit">
                    <i class="fa fa-search"></i>
                    </button>
                </div>
            </div>
        </form>
     

        <div class="navbar-nav">
            <div class="widget-header">
                <% if current_user%>
                    <span class="text-menu">
                        <%= link_to "Cerrar Sesión", destroy_user_session_path, method: :delete, class: "text-menu"%>
                    </span>   
                    <%= current_user.email%>
                <% else %>
				<!--<small class="title text-muted">Bienvenido!</small>-->
                    <div class="sign-login">
                        <%= link_to "Ingresar", new_user_session_path, class:"text-menu"%>
                        <span class="bar-transparent">|</span>
                        <%= link_to "Registrate", new_user_registration_path, class:"text-menu"%>
                    </div>
                <% end %>
			</div>
        </div>
        <div class="input-group-append">
            <button class="btn btn-shopping-cart" type="submit">
                <i class="fa fa-shopping-basket"></i>
            </button>
        </div>

    </div>
</nav>
<script>
    const input = document.getElementById("search-input");
    const searchBtn = document.getElementById("search-btn");

    const expand = () => {
    searchBtn.classList.toggle("close");
    input.classList.toggle("square");
    };

    searchBtn.addEventListener("click", expand);
</script>